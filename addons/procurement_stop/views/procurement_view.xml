<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data>
        <!--
        Procurement
        -->


        <!--<record id="procurement_tree_view" model="ir.ui.view">
            <field name="name">procurement.order.tree</field>
            <field name="model">procurement.order</field>
            <field name="arch" type="xml">
                <tree string="Procurement Lines" decoration-danger="date_planned&lt;current_date and state == 'exception'" decoration-success="state=='confirmed'" decoration-muted="state in ['done','cancel']" decoration-info="state == 'ready'">
                    <field name="date_planned" widget="date"/>
                    <field name="origin"/>
                    <field name="product_id"/>
                    <field name="product_qty"/>
                    <field name="product_uom" string="Unit of Measure"/>
                    <field name="state"/>
                </tree>
            </field>
        </record>-->

        <record id="procurement_form_view_inherit_stop" model="ir.ui.view">
            <field name="name">procurement.order.form.inherit.stop</field>
            <field name="model">procurement.order</field>
            <field name="inherit_id" ref="procurement.procurement_form_view"/>
            <field name="arch" type="xml">
                <button name="run" position="attributes">
                    <attribute name="attrs">{'invisible': [('do_stop', '!=', False)]}</attribute>
                </button>
                <button name="run" position="after">
                    <button name="run_continue" string="Unblock and Run" attrs="{'invisible': [('do_stop', '=', False)]}" type="object"/>
                </button>
                <field name="priority" position="after">
                    <field name="do_stop"/>
                </field>
            </field>
        </record>

        <!-- Procurement Rules -->

        <!--<record id="view_procurement_rule_filter" model="ir.ui.view">
            <field name="name">procurement.rule.select</field>
            <field name="model">procurement.rule</field>
            <field name="arch" type="xml">
                <search string="Search Procurement">
                    <filter string="Global" name="route_false" domain="[('route_id','=',False)]" help="If the route is global"/>
                </search>
            </field>
        </record>

        <record model="ir.ui.view" id="view_procurement_rule_tree">
            <field name="name">procurement.rule.tree</field>
            <field name="model">procurement.rule</field>
            <field eval="10" name="priority"/>
            <field name="arch" type="xml">
                <tree string="Procurement Rules">
                    <field name="name"/>
                    <field name="action"/>
                    <field name='company_id' groups="base.group_multi_company"/>
                </tree>
            </field>
        </record>-->

        <record model="ir.ui.view" id="view_procurement_rule_form_inherit_stop">
            <field name="name">procurement.rule.form</field>
            <field name="model">procurement.rule</field>
            <field name="inherit_id" ref="procurement.view_procurement_rule_form"/>
            <field name="arch" type="xml">
                <field name="action" position="after">
                    <field name="do_stop"/>
                </field>
            </field>
        </record>
    </data>
</openerp>
